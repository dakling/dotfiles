---
phase: 03-ship
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - doom/config.el
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements:
  - SC-01
  - SC-02

must_haves:
  truths:
    - "Package is active in doom/packages.el and doom/config.el with smoke test keybinding"
    - "After doom sync, the straight.el checkout has all Phase 2 features plus smoke test"
    - "Smoke test passes all 6 checks (package loaded, shim found, shim executable, server running, advice active, teammate-mode flag)"
    - "Spawning an agent team creates panes with headers and status indicators"
  artifacts:
    - path: "doom/config.el"
      provides: "Smoke test keybinding SPC o C T"
      contains: "claude-code-emacs-panes-smoke-test"
    - path: "~/.config/emacs/.local/straight/repos/claude-code-emacs-panes/claude-code-emacs-panes.el"
      provides: "Updated package checkout with smoke test"
      contains: "defun claude-code-emacs-panes-smoke-test"
  key_links:
    - from: "doom/config.el"
      to: "claude-code-emacs-panes-smoke-test"
      via: "SPC o C T keybinding"
      pattern: "\"T\" #'claude-code-emacs-panes-smoke-test"
    - from: "straight.el repos checkout"
      to: "origin/main on GitHub"
      via: "straight-pull-package or git pull"
      pattern: "git log shows smoke test commit"
---

<objective>
Integrate the updated package into the production dotfiles and verify end-to-end.

Purpose: Ensure the full feature set (Phases 1-3) works from the straight.el checkout, not just the dev checkout. This is the "new machine" path: doom sync pulls from GitHub, package loads, everything works.
Output: Verified production config with smoke test passing and live agent panes confirmed.
</objective>

<execution_context>
@/Users/darioklingenberg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darioklingenberg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ship/03-RESEARCH.md
@.planning/phases/03-ship/03-01-SUMMARY.md
@doom/config.el
@doom/packages.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update straight.el checkout, add smoke test keybinding, run doom sync</name>
  <files>
    doom/config.el
  </files>
  <action>
1. Pull the latest commits into straight.el's repos checkout:
   ```bash
   git -C ~/.config/emacs/.local/straight/repos/claude-code-emacs-panes pull origin main
   ```
   This should bring the checkout from `6ed9e86` (Phase 1 end) to the latest commit (with Phase 2 features + smoke test + README).

2. Add the smoke test keybinding to the `use-package!` block in `doom/config.el`. In the `map!` form under `(:prefix ("o C" . "claude panes"))`, add after the "Start Claude with panes" line:
   ```elisp
   :desc "Run smoke test" "T" #'claude-code-emacs-panes-smoke-test
   ```
   This binds SPC o C T (capital T).

3. Run `doom sync` to rebuild the package (byte-compile the updated .el file):
   ```bash
   ~/.config/emacs/bin/doom sync
   ```

4. Verify the straight.el build dir has the updated file:
   ```bash
   grep -c "smoke-test" ~/.config/emacs/.local/straight/build-*/claude-code-emacs-panes/claude-code-emacs-panes.el
   ```
   Should return a count > 0.

5. Verify the straight.el repos checkout is current:
   ```bash
   git -C ~/.config/emacs/.local/straight/repos/claude-code-emacs-panes log --oneline -3
   ```
   Latest commit should include "smoke test" in the message.

The existing `packages.el` declaration and `use-package!` block are already correct and active (verified in research). Only the smoke test keybinding line is new.

Per user decision: do NOT touch unrelated untracked files (amethyst.yml, test-edit.txt, doom/custom.el, doom/bin/). Only panes-related changes.
  </action>
  <verify>
```bash
grep "smoke-test" ~/.dotfiles/doom/config.el
```
Should show the new keybinding line.

```bash
git -C ~/.config/emacs/.local/straight/repos/claude-code-emacs-panes log --oneline -1
```
Should show the smoke test commit.

```bash
grep -c "defun claude-code-emacs-panes-smoke-test" ~/.config/emacs/.local/straight/build-*/claude-code-emacs-panes/claude-code-emacs-panes.el
```
Should return 1.
  </verify>
  <done>
- SPC o C T keybinding present in doom/config.el
- straight.el repos checkout is at the latest commit with smoke test
- doom sync completed successfully
- Build dir contains the updated .el file with smoke test function
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify smoke test and live agent panes</name>
  <what-built>
Complete claude-code-emacs-panes integration: package published to GitHub with smoke test and README, straight.el updated, doom sync'd, SPC o C T keybinding added.
  </what-built>
  <how-to-verify>
**Step 1: Reload Doom config in Emacs**
Run `M-x doom/reload` in your running Emacs instance.

**Step 2: Trigger package load**
Run `M-x claude-code-ide` (or `M-x claude-code-ide-menu`) to trigger the `:after-call` lazy load of claude-code-emacs-panes.

**Step 3: Run smoke test**
Press `SPC o C T` (or `M-x claude-code-emacs-panes-smoke-test`).
Check `*Messages*` buffer for results. All 6 checks should show PASS:
  - package loaded
  - shim found
  - shim executable
  - emacs server running
  - env-inject advice active
  - --teammate-mode in cli flags

**Step 4: Live agent verification**
In the Claude Code session, ask it to spawn subagents (e.g., "spawn 2 agents to work on separate tasks"). Verify:
  - Each agent gets its own vterm buffer
  - Buffer names include the agent identifier
  - Header line shows agent name + color-coded running indicator
  - `SPC o C d` opens the dashboard showing all agents
  - `SPC o C n` / `SPC o C p` cycles between panes
  - When agents finish, header line updates to finished indicator and buffer dims

**Step 5: Review keybindings**
Full list under SPC o C:
  - a: Show all panes
  - t: Toggle pane layout
  - n: Next pane
  - p: Previous pane
  - s: Select pane (completing-read)
  - d: Dashboard
  - K: Close finished panes
  - c: Start Claude with panes
  - T: Run smoke test

Confirm these match your expectations. If you want to change any keybindings, describe the change.
  </how-to-verify>
  <resume-signal>Type "approved" if smoke test passes and live panes work, or describe any issues to fix inline.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit dotfiles and finalize project</name>
  <files>
    doom/config.el
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <action>
After user approval from the checkpoint:

1. Update `.planning/ROADMAP.md`:
   - Change Phase 3 line from `- [ ]` to `- [x]`
   - Update Plans line to: `**Plans:** 2 plans`
   - Add plan list:
     ```
     Plans:
     - [x] 03-01-PLAN.md — Publish package with smoke test and README
     - [x] 03-02-PLAN.md — Integrate, verify, and ship
     ```
   - Update progress table: Phase 3 status to "Verified", Plans Complete to "2/2", add completion date

2. Update `.planning/STATE.md`:
   - Phase: 3 of 3 (Ship) -- VERIFIED
   - Status: Phase 3 verified — project complete
   - Progress: 100%
   - Current focus: Project complete

3. Commit panes-related dotfiles changes:
   - Stage ONLY: `doom/config.el`, `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/phases/03-ship/` (all plan and summary files)
   - Also stage the verification files from prior phases if they exist as untracked: `.planning/phases/01-core-reliability/01-VERIFICATION.md`, `.planning/phases/02-agent-lifecycle/02-VERIFICATION.md`
   - Do NOT stage: `amethyst.yml`, `test-edit.txt`, `doom/custom.el`, `doom/bin/`, `CLAUDE.md`
   - Commit message: `feat(03-ship): ship claude-code-emacs-panes — smoke test, README, verified end-to-end`
  </action>
  <verify>
```bash
git -C /Users/darioklingenberg/.dotfiles log --oneline -1
```
Should show the ship commit.

```bash
git -C /Users/darioklingenberg/.dotfiles status --short
```
Should show only non-panes untracked files remaining (amethyst.yml, test-edit.txt, CLAUDE.md, doom/bin/, doom/custom.el).
  </verify>
  <done>
- doom/config.el committed with smoke test keybinding
- ROADMAP.md shows Phase 3 complete, all 3 phases checked off
- STATE.md shows 100% progress, project complete
- Planning artifacts (VERIFICATION.md files) committed
- Working tree has no panes-related uncommitted changes
  </done>
</task>

</tasks>

<verification>
- Smoke test passes all 6 checks in *Messages* buffer
- Live agent panes appear with correct visual treatment (headers, colors, dimming)
- All SPC o C keybindings functional
- Dotfiles committed with only panes-related changes
- ROADMAP shows project 100% complete
</verification>

<success_criteria>
The package is active in the production Doom Emacs config, verified working via smoke test (6/6 checks pass) and live agent interaction, published to GitHub with README, and the dotfiles repo is committed cleanly with the project marked complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ship/03-02-SUMMARY.md`
</output>
