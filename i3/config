# Default config for i3
#
# Copy this to ~/.config/i3/config and edit it to your liking.
font xft:DejaVu Sans 10
### Variables
#
# Logo key. Use Mod1 for Alt.
set $mod Mod4
# Home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l
# Your preferred terminal emulator
set $term termite
# set $editor nvim-termite
set $editor nvim-termite
# Your preferred application launcher
#set $menu dmenu_extended_run

### Key bindings
#
# Basics:
#
    # start a terminal
    bindsym $mod+Return exec $term
    bindsym $mod+Shift+Return exec $term -d "`xcwd`"

    # kill focused window
    bindsym $mod+c kill

    # start your launcher
    bindsym $mod+d exec --no-startup-id albert show
    bindsym $mod+q exec dmenu_recency
    #bindsym $mod+shift+d exec albert show
    #bindsym $mod+ctrl+d exec --no-startup-id xlunch

    # Drag floating windows by holding down $mod and left mouse button.
    # Resize them with right mouse button + $mod.
    # Despite the name, also works for non-floating windows.
    # Change normal to inverse to use left mouse button for resizing and right
    # mouse button for dragging.
    #floating_modifier $mod normal

    # reload the configuration file
    bindsym $mod+Shift+r restart
    bindsym $mod+Ctrl+r reload

    # exit sway (logs you out of your wayland session)
    bindsym $mod+Shift+e exit
#
# Moving around:
#
set $follow exec xdotool mousemove --window $(xdotool getactivewindow) --polar 0 0
    # Move your focus around
    bindsym $mod+$left focus left; $follow
    bindsym $mod+$down focus down; $follow
    bindsym $mod+$up focus up; $follow
    bindsym $mod+$right focus right; $follow
    # or use $mod+[up|down|left|right]
    bindsym $mod+Left focus left; $follow
    bindsym $mod+Down focus down; $follow
    bindsym $mod+Up focus up; $follow
    bindsym $mod+Right focus right; $follow
    # _move_ the focused window with the same, but add Shift
    bindsym $mod+Shift+$left move left; $follow
    bindsym $mod+Shift+$down move down; $follow
    bindsym $mod+Shift+$up move up; $follow
    bindsym $mod+Shift+$right move right; $follow
    # ditto, with arrow keys
    bindsym $mod+Shift+Left move left; $follow
    bindsym $mod+Shift+Down move down; $follow
    bindsym $mod+Shift+Up move up; $follow
    bindsym $mod+Shift+Right move right; $follow
#Resize outside of the mode
    bindsym $mod+Ctrl+$left resize shrink width 10 px or 10 ppt
    bindsym $mod+Ctrl+$down resize grow height 10 px or 10 ppt
    bindsym $mod+Ctrl+$up resize shrink height 10 px or 10 ppt
    bindsym $mod+Ctrl+$right resize grow width 10 px or 10 ppt
# Workspaces:
#
    # switch to workspace
    bindsym $mod+1 workspace 1
    bindsym $mod+2 workspace 2
    bindsym $mod+3 workspace 3
    bindsym $mod+4 workspace 4
    bindsym $mod+5 workspace 5
    bindsym $mod+6 workspace 6
    bindsym $mod+7 workspace 7
    bindsym $mod+8 workspace 8
    bindsym $mod+9 workspace 9
    bindsym $mod+0 workspace 10
    # move focused container to workspace
    bindsym $mod+Shift+1 move container to workspace 1; workspace 1
    bindsym $mod+Shift+2 move container to workspace 2; workspace 2
    bindsym $mod+Shift+3 move container to workspace 3; workspace 3
    bindsym $mod+Shift+4 move container to workspace 4; workspace 4
    bindsym $mod+Shift+5 move container to workspace 5; workspace 5
    bindsym $mod+Shift+6 move container to workspace 6; workspace 6
    bindsym $mod+Shift+7 move container to workspace 7; workspace 7
    bindsym $mod+Shift+8 move container to workspace 8; workspace 8
    bindsym $mod+Shift+9 move container to workspace 9; workspace 9
    bindsym $mod+Shift+0 move container to workspace 10; workspace 10
    # Note: workspaces can have any name you want, not just numbers.
    # We just use 1-10 as the default.
#
# Layout stuff:
#
    # You can "split" the current object of your focus with
    # $mod+b or $mod+v, for horizontal and vertical splits
    # respectively.
    bindsym $mod+b splith
    bindsym $mod+v splitv

    # Switch the current container between different layout styles
    #bindsym $mod+s layout stacking
    bindsym $mod+s layout tabbed
    bindsym $mod+e layout toggle split

    # Make the current focus fullscreen
    bindsym $mod+f fullscreen

    # Toggle the current focus between tiling and floating mode
    bindsym $mod+Shift+space floating toggle

    # Swap focus between the tiling area and the floating area
    bindsym $mod+space focus mode_toggle

    # move focus to the parent container
    bindsym $mod+a focus parent
#
# Scratchpad:
#
    # Sway has a "scratchpad", which is a bag of holding for windows.
    # You can send windows there and get them back later.

    # Move the currently focused window to the scratchpad
    bindsym $mod+minus move scratchpad

    # Show the next scratchpad window or hide the focused scratchpad window.
    # If there are multiple scratchpad windows, this command cycles through them.
    bindsym $mod+Shift+minus scratchpad show; $follow
#
# Resizing containers:
#
mode "resize" {
    # left will shrink the containers width
    # right will grow the containers width
    # up will shrink the containers height
    # down will grow the containers height
    bindsym $left resize shrink width 10 px or 10 ppt
    bindsym $down resize grow height 10 px or 10 ppt
    bindsym $up resize shrink height 10 px or 10 ppt
    bindsym $right resize grow width 10 px or 10 ppt

    # ditto, with arrow keys
    bindsym Left resize shrink width 10 px or 10 ppt
    bindsym Down resize grow height 10 px or 10 ppt
    bindsym Up resize shrink height 10 px or 10 ppt
    bindsym Right resize grow width 10 px or 10 ppt

    # return to default mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

#
# Status Bar:
#
# Read `man 5 sway-bar` for more information about this section.

set $active_text_color #00ADEE
set $active_bg_color #181818
set $active_border_color #00ADEE
set $inactive_text_color #c1c8d1
set $inactive_bg_color #181818
set $inactive_border_color #181818
set $bg_color #383c4a 
bar {
    position top
        status_command i3status
        # status_command py3status
        ## please set your primary output first. Example: 'xrandr --output eDP1 --primary'
        tray_output primary
        #strip_workspace_numbers yes
        colors {
            background $transparent
                background $active_bg_color
                statusline $active_text_color
                separator  #454947

#                                  border  backgr. text
                focused_workspace  $active_border_color $active_bg_color $active_text_color
                active_workspace   $inactive_border_color $active_bg_color $active_text_color
                inactive_workspace $inactive_border_color $inactive_bg_color $inactive_text_color
                urgent_workspace   $active_border_color $active_bg_color $active_text_color
        }
}
# Start Applications
bindsym $mod+F1 exec emacs ~
bindsym $mod+F2 exec qutebrowser --backend webengine
bindsym $mod+F3 exec rangerStandalone
bindsym $mod+Shift+F3 exec rangerStandalone "`xcwd`"
bindsym $mod+F4 exec $term -e mutt
bindsym $mod+F5 exec $term -e pacui
bindsym $mod+F9 exec --no-startup-id /usr/local/bin/kb-light.py +
bindsym $mod+F8 exec --no-startup-id /usr/local/bin/kb-light.py -
bindsym XF86AudioRaiseVolume exec changeVolume +5
bindsym XF86AudioLowerVolume exec changeVolume -5
bindsym XF86AudioMute exec pactl set-sink-mute $(pacmd list-sinks |awk '/* index:/{print $3}') toggle
bindsym XF86MonBrightnessDown exec dsplight down 5
bindsym XF86MonBrightnessUp exec dsplight up 5
bindsym print exec i3scrot -s

focus_follows_mouse yes
mouse_warping output
#border style
#default_border pixel 4
new_window pixel 4
new_float normal
#Custom modes
#Program starter
mode "Quick_Program_Starter" {
bindsym i      exec firefox; mode default
bindsym e      exec thunderbird; mode default
bindsym g      exec emacs; mode default
bindsym d      exec pcmanfm; mode default
bindsym s      exec lxappearance; mode default
bindsym p      exec pamac-manager; mode default
bindsym t      exec $term; mode default
bindsym m      exec $term -e cmus; mode default
bindsym a      exec $term -e pulsemixer; mode default
bindsym c      exec nmcli_dmenu; mode default
bindsym Return mode "default"
bindsym Escape mode "default"
}
bindsym $mod+x mode "Quick_Program_Starter"
#File opener
bindsym $mod+Shift+x mode "Quick_File_Opener"
mode "Quick_File_Opener" {
bindsym e      exec $term -e "python /home/dario/Dropbox/Helen/einkaufsliste/interactiveEnterDario.py"; mode default
bindsym c      exec $editor $HOME/.config/i3/config; mode default
bindsym v      exec $editor $HOME/.config/nvim/init.vim; mode default
bindsym s      exec $editor $HOME/.spacemacs; mode default
bindsym q      exec $editor /home/dario/.config/qutebrowser/qutebrowser.conf; mode default
bindsym t      exec $editor /home/dario/.config/termite/config; mode default
bindsym z      exec $editor $HOME/.zshrc; mode default
bindsym Return mode "default"
bindsym Escape mode "default"
}
set $mode_file_opener Quick File Launcher
# Set shut down, restart and locking features
bindsym $mod+w mode "Exit"
mode "Exit" {
    bindsym l exec --no-startup-id i3exit lock, mode "default"
    bindsym b exec --no-startup-id i3exit suspend, mode "default"
    bindsym u exec --no-startup-id i3exit switch_user, mode "default"
    bindsym e exec --no-startup-id i3exit logout, mode "default"
    bindsym h exec --no-startup-id i3exit hibernate, mode "default"
    bindsym r exec --no-startup-id i3exit reboot, mode "default"
    bindsym s exec --no-startup-id i3exit shutdown, mode "default"
    # exit system mode: "Enter" or "Escape"
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
# Workspaces
workspace 1 output LVDS1
workspace 3 output LVDS1
workspace 5 output LVDS1
workspace 7 output LVDS1
workspace 9 output LVDS1
workspace 2 output VGA1
workspace 4 output VGA1
workspace 6 output VGA1
workspace 8 output VGA1
workspace 10 output VGA1

#autostart
exec --no-startup-id /usr/lib/polkit-kde/polkit-kde-authentication-agent-1
exec --no-startup-id sleep 1; compton -b
exec --no-startup-id nm-applet
exec --no-startup-id pamac-tray
#exec --no-startup-id pa-applet
exec --no-startup-id clipit
exec --no-startup-id dropbox
# exec --no-startup-id redshift-gtk;
exec --no-startup-id easystroke
exec --no-startup-id indicator-kdeconnect
# exec --no-startup-id onboard
#exec --no-startup-id xautolock -time 5 -locker blurlock
exec --no-startup-id setxkbmap -option ctrl:nocaps; sleep 1; xcape -e 'Control_L=Escape'  
exec_always --no-startup-id fix_xcursor
exec --no-startup-id monitor_home
exec --no-startup-id albert &

# Window rules
for_window [title="CGoban: Main Window"] floating disable
