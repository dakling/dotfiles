# SOPS configuration
# Add your age public key here (from age-keygen)

keys:
  - &laptop age1YOUR_AGE_PUBLIC_KEY_HERE

creation_rules:
  - path_regex: secrets/[^/]+\.yaml$
    key_groups:
      - age:
          - *laptop