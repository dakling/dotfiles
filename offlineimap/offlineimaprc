[general]
accounts = gmail, web, fdy, gsc
pythonfile = ~/.offlineimap.py

[Account gmail]
localrepository = gmail-loc
remoterepository = gmail-rem

[Account web]
localrepository = web-loc
remoterepository = web-rem

[Account fdy]
localrepository = fdy-loc
remoterepository = fdy-rem

[Account gsc]
localrepository = gsc-loc
remoterepository = gsc-rem

[Repository gmail-loc]
type = Maildir
localfolders = ~/Mail/Gmail/

[Repository gmail-rem]
type = Gmail
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
ssl_version = tls1_2
remotehost = imap.gmail.com
remoteuser = dario.klingenberg@gmail.com
remotepasseval = get_pass_gmail()
# nametrans = lambda foldername: re.sub ('^\[gmail\]', 'bak',
#                                re.sub ('sent_mail', 'sent',
#                                re.sub ('starred', 'flagged',
#                                re.sub (' ', '_', foldername.lower()))))
# folderfilter = lambda foldername: foldername not in ['[Gmail]/All Mail']

[Repository web-loc]
type = Maildir
localfolders = ~/Mail/Web/

[Repository web-rem]
type = IMAP
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
ssl_version = tls1_2
remotehost = imap.web.de
remoteuser = dario.klingenberg@web.de
remotepasseval = get_pass_web()

[Repository fdy-loc]
type = Maildir
localfolders = ~/Mail/FDY/

[Repository fdy-rem]
type = IMAP
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
ssl_version = tls1_2
remotehost = mail.tu-darmstadt.de
remoteuser = km88econ
remotepasseval = get_pass_fdy()

[Repository gsc-loc]
type = Maildir
localfolders = ~/Mail/GSC/

[Repository gsc-rem]
type = IMAP
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
ssl_version = tls1_2
remotehost = mail.gsc.ce.tu-darmstadt.de
remoteuser = klingenberg@gsc.tu-darmstadt.de
remotepasseval = get_pass_gsc()
